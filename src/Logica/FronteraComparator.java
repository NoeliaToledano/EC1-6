package Logica;

import Recursos.*;

import java.util.*;

public class FronteraComparator implements Comparator<Sucesor> {


    public int compare(Sucesor s1, Sucesor s2) {

        /*No se pueden comparar sucesores mediante la f porque no tienen,
        en tod.o caso habría que comparar Nodos..

        if (s1. < s2.cgpa)
            return 1;
        else if (s1.cgpa > s2.cgpa)
            return -1;
        return 0;

         */
        return 0;
    }
}